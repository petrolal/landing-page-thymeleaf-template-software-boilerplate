spring:
  application:
    name: landing-page-thymeleaf-template-first

  # datasource:
  #   url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:landing_db}
  #   username: ${DB_USERNAME:postgres}
  #   password: ${DB_PASSWORD:postgres}
  #   driver-class-name: org.postgresql.Driver
  #
  # jpa:
  #   hibernate:
  #     ddl-auto: validate
  #   show-sql: true
  #   open-in-view: false
  #   properties:
  #     hibernate:
  #       format_sql: true
  #       dialect: org.hibernate.dialect.PostgreSQLDialect
  #
  # flyway:
  #   enabled: true
  #   baseline-on-migrate: true
  #   locations: classpath:db/migration

server:
  port: ${PORT:8080}
